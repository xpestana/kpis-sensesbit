# Base de datos (psycopg2)
POSTGRES_URL=postgresql://user:password@localhost:5432/kpis

# HubSpot (Private App - Access Token para lectura)
HUBSPOT_API_KEY=

# CORS (orígenes permitidos, separados por coma)
ORIGIN_HOSTS=http://localhost:9000,http://localhost:3000

# Opcional: schema global para tablas compartidas
GLOBAL_SCHEMA=shared

# Opcional: True en producción (oculta docs)
PROD=false
